import React from 'react';

const Header: React.FC = () => {
    return (
        <header className="h-[236px] bg-[#090D11] -z-10 fixed top-0 left-0 w-full"></header>
        
    );
};

export default Header;